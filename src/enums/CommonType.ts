export enum ToolType {
  None = "none",
  Shape = "shape",
  Fill = "fill"
}

export enum LayerType {
  Shape = "shape",
  Fill = "fill"
}

export enum ShapeType {
  Rect = "rect",
  Circle = "circle"
}
