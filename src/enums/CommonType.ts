export enum ToolType {
  None = "none",
  Shape = "shape",
  Fill = "fill",
  Brush = "brush"
}

export enum LayerType {
  Shape = "shape",
  Fill = "fill",
  Brush = "brush"
}

export enum ShapeType {
  Rect = "rect",
  Circle = "circle"
}
